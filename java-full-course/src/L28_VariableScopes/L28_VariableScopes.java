package L28_VariableScopes;
public class L28_VariableScopes {
    public static void main(String[] args) {
        // local = declared inside a method and visible only to that method
        // global = declared outside a method, but within a class and is visible to all parts of a class

        DiceRoller diceRoller = new DiceRoller();
    }
}
